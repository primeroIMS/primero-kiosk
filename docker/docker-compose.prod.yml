---
version: '3'
services:
  application:
    env_file:
      - docker/defaults.env
      - docker/local.env
  worker:
    volumes:
      - public:/srv/primero/application/public
    env_file:
      - docker/defaults.env
      - docker/local.env

