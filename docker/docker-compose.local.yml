version: '3'
services:
  application:
    env_file:
      - docker/defaults.env
      - docker/local.env
    ports:
      - "3000:3000"
  worker:
    env_file:
      - docker/defaults.env
      - docker/local.env
