/* eslint-disable */
<%= banner %>

import { I18n } from "i18n-js";

const i18n = new I18n();

i18n.store(<%= JSON.pretty_generate(translations) %>);

export type I18nLocale = <%= I18n.available_locales.map { |locale| "'#{locale}'" }.join(" | ") %>;
export default i18n;